---
const { name, slug, price, image, description, marca } = Astro.props;
---

          <div class="p-4 bg-white hover:shadow-lg transition-shadow">
            <div class="aspect-square mb-4 flex items-center justify-center bg-gray-50 rounded-lg">
              <img src={image} alt={name} class="max-w-full max-h-full object-contain p-4" />
            </div>
            <div class="flex justify-between">
              <a href={`/productos/${slug}`} class="text-sm font-semibold mb-2 h-8 line-clamp-2">{name}</a>
              <span  class="text-sm font-sans text-primary mb-2 h-8 line-clamp-2">{marca}</span>
            </div>
            <p class="text-sm font-normal line-clamp-2 mb-2 max-w-xs truncat">{description}</a>
            <p class="text-lg font-bold text-primary mb-3">S/ {price}</p>
            <a href={`/productos/${slug}`} class="w-full bg-primary text-white rounded-lg px-4 py-2 hover:bg-primary-dark transition ">
              Ver Producto
            </a>
          </div>